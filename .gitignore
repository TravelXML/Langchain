__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

.env
data/*.db
data/*.sqlite
logs/*.log

# Uploaded resumes/cover letters contain candidate PII — never committed.
data/uploads/

# Browser session cookies and failure screenshots/HTML can contain PII or
# session tokens — never committed.
data/browser_sessions/
data/browser_artifacts/

# Source master prompt — was previously leaked into a commit and scrubbed
# from history (2026-08-13); this guards against it recurring.
/prompt/

.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

node_modules/
frontend/dist/

.DS_Store
